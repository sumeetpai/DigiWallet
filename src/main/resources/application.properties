#REVIEW : JPA : 1
spring.application.name=DigiWallet
spring.datasource.url=jdbc:mysql://localhost:3306/digital_wallet_db
spring.datasource.username=root
spring.datasource.password=n3u3da!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


spring.jpa.hibernate.ddl-auto=update
#validate, update, create, create-drop
#validate: validates the schema, makes no changes to the database.
#update: updates the schema.
# create: creates the schema, destroying previous data.
# create-drop: creates the schema on startup, destroys it on shutdown.

spring.jpa.show-sql=true
# show-sql means to print the generated SQL statements to the console.
spring.jpa.properties.hibernate.format_sql=true
# format_sql means to format the SQL statements for better readability.
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
# Specifies the SQL dialect to use for MySQL database.
# This helps Hibernate generate optimized SQL queries for MySQL.


spring.sql.init.mode=never
